# Dependencies
node_modules/

# Build output (regenerated by `npm run build`; Cloudflare Pages builds from source)
dist/
dist-server/

# Local env (real values live in Cloudflare Pages env vars; see .env.example)
.env
.env.local
.env.*.local

# Misc
.DS_Store
*.log
